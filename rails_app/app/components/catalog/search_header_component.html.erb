<%# Overriding component from Blacklight v9.0.0 to combine constraints/sort into one nav %>

<nav class="fa-constraints-and-sort">
  <%= render helpers
    .blacklight_config
    .view_config(helpers.document_index_view_type)
    .constraints_component
    .new(search_state: helpers.search_state, start_over_component: Catalog::StartOverButtonComponent) %>
  <%= render "sort_and_per_page" %>
</nav>
