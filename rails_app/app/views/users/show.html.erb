<h1 class="mb-3">User Summary</h1>

<div class="btn-toolbar mb-4" role="toolbar">
  <div class="btn-group me-2">
    <%= link_to t('admin.actions.edit'), edit_user_path(@user), class: 'btn btn-outline-dark' %>
  </div>
  <div class="btn-group">
    <%= link_to t('admin.actions.destroy'), user_path(@user), class: 'btn btn-outline-danger', method: :delete %>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <dl class="mb-0">
      <dt>PennKey</dt>
      <dd><%= @user.uid %></dd>

      <dt>Email</dt>
      <dd><%= @user.email %></dd>

      <dt>Created At</dt>
      <dd><%= @user.created_at.to_fs(:display) %></dd>

      <dt>Active Status</dt>
      <dd><%= @user.active ? 'Active' : 'Inactive' %></dd>
    </dl>
  </div>
</div>
