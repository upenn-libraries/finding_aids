<%= bootstrap_form_for(@user) do |f| %>
  <%= f.text_field :uid, label: t('admin.users.uid') %>
  <%= f.email_field :email, disabled: true %>
  <%= f.check_box :active %>
  <%= f.submit 'Save' %>
<% end %>