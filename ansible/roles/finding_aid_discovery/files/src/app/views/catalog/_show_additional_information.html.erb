<% %w[biog_hist scope_content arrangement].each do |section| %>
  <%= render AdditionalInformationComponent.new title: t(section, scope: :sections), node: document.parsed_ead.try(section) %>
<% end %>

<%= render(CollectionsInventoryComponent.new(node: document.parsed_ead.dsc)) %>
